local cut = require(script.functions.cut)
local merge = require(script.functions.merge)
local reverse = require(script.functions.reverse)
local shuffle = require(script.functions.shuffle)

return {
	reverse = reverse,
	cut = cut,
	merge = merge,
	shuffle = shuffle,
}
